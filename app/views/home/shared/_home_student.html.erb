<div id="principal_student" class="course scrollspyTitle title_sub">
    <div class="container">
        <div class="row">
            <div class="col l12 m12 s12 center-align">
                <h1 id="hero_title" style="color:white"><strong class="bold">¡Hola, estudiante!, </strong>
                    <% if current_user.accept_model == true %>
                    <% if !current_user.group.present? %>
                    <span class="light">¡Muchas gracias por responder! Ahora debes esperar a que el profesor arme los grupos de trabajo.</span>
                    <% else %>
                    <span class="light">¡Ya tienes equipo de trabajo! Si quieres ir a verlo <%= link_to 'haz clic aquí.',mi_equipo_path %></span>
                    <% end %>
                </h1>
            </div>
        </div>
    </div>
</div>
<% end %>